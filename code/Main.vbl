from vbcell import *

function add(a,b)
    return a + b
end

function main()
    
    $(0,0) = 1
    $(1,0) = 2

    $(2,0) = $(0,0) + $(1,0)
    $(3,0) = add($(0,0) + $(0,0),$(1,0) + $(1,0))
    
    i = 0
    while i<5
        $(i,i) = "Index: " + i
        i = i + 1
    end
    
    return
end